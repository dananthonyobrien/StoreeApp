<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="org.wit.placemark.views.placemarklist.PlacemarkListView">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FF8800"
        android:fitsSystemWindows="true"
        app:elevation="0dip"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:titleTextColor="@color/colorPrimary" />
    </com.google.android.material.appbar.AppBarLayout>

   <!-- <fragment
        android:id="@+id/fragment_quote"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph" /> -->

    <!--android:name="androidx.navigation.fragment.NavHostFragment"-->


    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="604dp"
        android:gravity="center"
        android:text="@string/background_text"
        android:textColor="@color/colorAccent"
        android:textSize="21dp"
        app:layout_anchor="@+id/recyclerView"
        app:layout_anchorGravity="center"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="673dp"
        android:scrollbars="vertical"
        app:layout_anchor="@+id/recyclerView"
        app:layout_anchorGravity="center"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/item_add1"
        android:layout_gravity="bottom|center"
        android:layout_margin="16dp"
        app:maxImageSize="150dp"
        app:backgroundTint="@android:color/holo_orange_dark"
        android:paddingTop="8dp"
        app:elevation="1dp"
        app:borderWidth="0dp"
        app:pressedTranslationZ="1dp"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:fabCustomSize="100dp"
        android:paddingBottom="8dp"
        android:src="@drawable/ic_fab5_icon_foreground"
        android:background="@android:color/holo_orange_dark"
        android:stateListAnimator="@null"
        android:textColor="@color/colorPrimary"
        android:textSize="16sp"
        android:tooltipText="Add Story"
        app:showAsAction="always" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>